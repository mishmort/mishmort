import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import MishMort from './MishMort';

ReactDOM.render(
  <React.StrictMode>
    <MishMort />
  </React.StrictMode>,
  document.getElementById('root')
);
